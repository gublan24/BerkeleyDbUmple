namespace com.sleepycat.je.evictor;

class Evictor
{
   after Label389: evictIN(IN , IN , int , INList , ScanIterator , boolean )
   {
	evictBytes = renewedChild.getInMemorySize();
	//return original(evictBytes, renewedChild);
    }

}
