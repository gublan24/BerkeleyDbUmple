
namespace com.sleepycat.je.recovery;
class Checkpointer {

      after Label517: isRunnable(CheckpointConfig ) {  // label introduced in loggingFinestCheckpointer_inner
        sb.append("size interval=").append(useBytesInterval);

      
    }
}
