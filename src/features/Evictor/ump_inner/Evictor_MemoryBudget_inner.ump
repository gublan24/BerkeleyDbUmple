
namespace com.sleepycat.je.dbi;

class MemoryBudget {

      after Label350:reset(DbConfigManager ) {
						;
						long newCriticalThreshold =
            (newMaxMemory * 
             envImpl.getConfigManager().getInt
                (EnvironmentParams.EVICTOR_CRITICAL_PERCENTAGE))/100;

      
    }
}
