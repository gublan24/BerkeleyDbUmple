namespace com.sleepycat.je.log;

class LastFileReader
{
    after Label477: initStartingPosition(long , Long ){
	fileHandle.release();
	//original(fileHandle);
    }

    after Label478:initStartingPosition(long , Long ){
	if (fileHandle != null) {
	    fileHandle.release();
	}
	//original(fileHandle);
    }

}
