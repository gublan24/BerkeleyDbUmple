namespace com.sleepycat.je.evictor;

class Evictor
{
  after Label386: selectIN(INList , ScanIterator ){
	b2 = db.isDeleted();

    }

  after Label387: selectIN(INList , ScanIterator ){
	b |= db.isDeleteFinished();

    }

}
