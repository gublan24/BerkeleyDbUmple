
namespace com.sleepycat.je.cleaner;

class FileProcessor {

      after Label135: processLN(Long , TreeLocation , Long , LNInfo , LookAheadCache , Map ) {
        			if (parentDIN != null) {
				parentDIN.releaseLatchIfOwner();
			}

			if (bin != null) {
				bin.releaseLatchIfOwner();
			}

      }
    
}
