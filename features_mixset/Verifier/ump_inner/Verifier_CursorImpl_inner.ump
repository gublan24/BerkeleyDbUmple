
namespace com.sleepycat.je.dbi;
class CursorImpl {

      after Label279: getNextDuplicate(DatabaseEntry , DatabaseEntry , LockType ,  boolean , boolean ) {
        		if (DEBUG) {
								verifyCursor(dupBin);
						}
      }
    
}
