
namespace com.sleepycat.je;

class Database {


      after Label39:  truncate(Transaction , boolean ) { 

				Tracer.trace(Level.FINEST, 
                     envHandle.getEnvironmentImpl(),
                     "Database.truncate: txnId=" + 
                     ((txn == null) ? "null" : Long.toString(txn.getId())));

      }
    
}
