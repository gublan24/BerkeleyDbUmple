namespace com.sleepycat.je.evictor;

class Evictor
{
    private Level detailedTraceLevel;

    after Label373: Evictor(EnvironmentImpl , String ){
			detailedTraceLevel = Tracer.parseLevel(envImpl, EnvironmentParams.JE_LOGGING_LEVEL_EVICTOR);

    }

}
