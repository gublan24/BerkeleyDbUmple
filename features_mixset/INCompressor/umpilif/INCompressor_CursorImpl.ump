namespace com.sleepycat.je.dbi;

class CursorImpl
{
    after Label281: delete() {
			locker.addDeleteInfo(dupBin, new Key(lnKey));
			//original(lnKey);
    }

    after Label282: delete() {
			locker.addDeleteInfo(bin, new Key(lnKey));
			//original(lnKey);
    }

}
