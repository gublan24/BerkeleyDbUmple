namespace com.sleepycat.je.log;

class LogManager
{
  after Label501:log(LoggableObject , boolean , boolean , boolean , boolean , long ){
	if (fsyncRequired) {
	    fileManager.groupSync();
	}

    }

}
