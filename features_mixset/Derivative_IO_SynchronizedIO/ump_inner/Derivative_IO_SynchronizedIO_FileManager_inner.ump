
class FileManager {

     around Label447-Label447_1: writeToFile(RandomAccessFile , ByteBuffer ,  long ) {
			synchronized (file) {
				around_proceed:
        }
      }
    

      around Label448-Label448_1: readFromFile(RandomAccessFile ,  ByteBuffer ,  long ) {
				synchronized (file) {
					around_proceed:
        }
      
    }
}
