
namespace com.sleepycat.je.txn;

class LockManager {


      after Label769: lockStat(StatsConfig ) {

					stats.setNRequests(nRequests);
		      stats.setNWaits(nWaits);
		      if (config.getClear()) {
		          nWaits = 0;
		          nRequests = 0;
		      }

					Label769_1:;

		      /* Dump info about the lock table. */
		      if (!config.getFast()) {
		          dumpLockTable(stats);
		      }
        
      }
    

    
}
